/*
** EPITECH PROJECT, 2023
** GraphiC
** File description:
** header file for maths functions
*/

#ifndef MATHS
    #define MATHS

    #define MIN(a, b)       ((a) < (b) ? (a) : (b))
    #define MAX(a, b)       ((a) > (b) ? (a) : (b))
    #define CLAMP(a, x, b)  MIN(b, MAX(x, a))

#endif
