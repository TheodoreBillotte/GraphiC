Sort sprites:
    V - using Z-Layering
    V - can change layer for texts, actors and buttons
    V - can change scene for texts, actors and buttons
    X - list of visible layer with int and each bit is a bool for is_visible or
        is updated and each layer is affected by the layer mask

Actors:
    V - Add update method to all actors

Inputs:
    X - Add support for text input

Slider:
    V - Add support for number slider

Dropdown:
    X - Add support for dropdown

Buttons:
    V - On Enter and On Leave for mouse instead of On Hover
    V - On Release
